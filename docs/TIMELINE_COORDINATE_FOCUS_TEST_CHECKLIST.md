# Timeline 坐标与聚焦测试清单

依据：`TIMELINE_COORDINATE_FOCUS_SPEC.md`

目标：验证 Timeline 的 `L(Left) / R(Right) / C(Cursor)`、聚焦切换、文本联动、播放快照、Follow 和停止位行为是否符合规格。

## 0. 测试准备

- [ ] 使用包含多行、多个逗号、不同节奏段的谱面。
- [ ] 准备一份“无任何逗号”的极端谱面文本。
- [ ] 准备一份“某行有起始时间但该行无逗号”的谱面文本。
- [ ] 确认 Timeline 中能同时看到：
  - [ ] `R` 竖线
  - [ ] `C` 竖线
  - [ ] `L` 三角标记
- [ ] 确认 Preview 控件可正常播放、暂停、停止、拖动 slider。

## 1. 基础语义

- [ ] 初始打开谱面后，`C` 跟随当前编辑器光标落到最近过去逗号。
- [ ] 初始未播放前，`L` 与 `R` 都在 `0` 或当前已 seek 的位置上表现合理。
- [ ] Timeline 中 `R` 和 `C` 颜色区分明显。
- [ ] `L` 显示为 header 下沿的三角标记，而不是竖线。

## 2. 文本侧 `C` 锚点规则

### 2.1 普通点击文字

- [ ] 点击某个逗号之后的位置时，`C` 落到该逗号时刻。
- [ ] 点击某行第一个逗号之前的位置时，`C` 回退到前一处最近过去逗号。
- [ ] 点击全文第一个逗号之前的位置时，`C := 0`。
- [ ] 点击文本不会改动 `R`。
- [ ] 暂停态普通点击文字后，Timeline 聚焦切到 `C`。

### 2.2 键盘移动光标

- [ ] 左右方向键移动编辑器光标时，`C` 总是取“最近过去逗号”。
- [ ] 上下切换到没有逗号的行时，`C` 仍按全局最近过去逗号定位。
- [ ] 全文无逗号时，无论怎么移动光标，`C` 始终为 `0`。

### 2.3 修改文本

- [ ] 在暂停态插入或删除字符后，Timeline 立即按当前文本重绘。
- [ ] 修改文本后，`C` 按“当前文本 + 当前光标”重算。
- [ ] 删除当前光标之前的最近逗号后，`C` 会回退到更早的逗号或 `0`。
- [ ] 当前文本短暂不可解析时，不会把 `R` 改掉；`C` 仍有“最近已知逗号 / 0”兜底。

## 3. `Ctrl+点文字` 的 `C -> R`

- [ ] 暂停态 `Ctrl+点文字` 后，先按点击位置确定 `C`，再令 `R := C`。
- [ ] 此时 Timeline 聚焦切到 `C`。
- [ ] `L` 不变。
- [ ] 播放中 `Ctrl+点文字` 会先暂停，再执行上述行为。

## 4. Timeline 头部点击与 `Ctrl+点 Timeline` 的 `R -> C`

### 4.1 头部点击

- [ ] 暂停态头部点击后，`R := 点击时刻`。
- [ ] 同时 `C` 按规格中的“最近过去行 + 该行第一个逗号，否则回退全局最近过去逗号”更新。
- [ ] Timeline 聚焦保持在 `R`。
- [ ] 头部点击不再吸附到最近 note 物件。

### 4.2 `Ctrl+点 Timeline`

- [ ] 暂停态 `Ctrl+点 Timeline` 与头部点击行为一致。
- [ ] 播放中 `Ctrl+点 Timeline` 会先暂停，再按点击时刻处理 `R / C`。
- [ ] 该行为不会跳到最近 note，而是严格按点击秒数定位。

## 5. Timeline 拖拽与滚轮

### 5.1 暂停态拖拽

- [ ] 当当前聚焦在 `C` 时，按下 Timeline 开始拖拽，`R` 会先对齐到画布中心时刻。
- [ ] 拖拽过程中真正变化的是 `R`，不是 `C`。
- [ ] 拖拽结束后，Timeline 仍聚焦 `R`。
- [ ] `L` 不变，`C` 不会被拖拽强行改写。

### 5.2 暂停态滚轮平移

- [ ] 滚轮平移时，行为与“纯 `R` 动作”一致。
- [ ] 如果开始时中心不是 `R`，会先把 `R` 对齐到中心，再继续平移。
- [ ] `C` 不应跟着滚轮平移被重写。

### 5.3 播放中拖拽 / 滚轮

- [ ] 播放中开始拖拽 Timeline 会先暂停。
- [ ] 暂停后执行同样的 `R` 对齐与拖动逻辑。
- [ ] `L` 保持本次播放入口，不会被拖拽改掉。

## 6. Preview Slider

- [ ] 暂停态拖动 slider 时，直接 `R := slider 时刻`。
- [ ] Slider 拖动不会走“先对齐视口中心”的逻辑。
- [ ] Slider 拖动不会自动改 `C`。
- [ ] 播放中拖动 slider 会先暂停，再设置 `R`。

## 7. 播放 / 暂停 / 停止位

### 7.1 点击播放

- [ ] 从暂停-R 点击播放时，`L := 当前 R`。
- [ ] 从暂停-C 点击播放时，`L := 当前 R`，不是 `L := C`。
- [ ] 播放开始后，Timeline 聚焦切到 `R`。
- [ ] 若 Follow 开启，开播后 `C` 立即绑定到 `<= R` 的最近过去逗号。
- [ ] 若 Follow 关闭，开播后 `C` 保持原值。

### 7.2 点击暂停

- [ ] 播放中点击暂停后，进入暂停-R。
- [ ] 暂停后 `R` 冻结在当前播放时刻。
- [ ] 暂停不会把 `R` 拉回 `L`。
- [ ] 暂停不会自动修改 `C`。

### 7.3 自然结尾

- [ ] 播放到结尾会停在结尾时刻，而不是回到 `L`。
- [ ] 自然结尾后为暂停-R语义。
- [ ] `C` 保持当时状态，不会额外跳动。

### 7.4 点击停止

- [ ] 点击停止后，`R := L`。
- [ ] 停止后仍属暂停-R语义。
- [ ] `C` 不变。
- [ ] 再次点击播放时，会从 `L` 处重新开始并刷新新的 `L` 标记。

## 8. Follow 行为

### 8.1 播放中打开 / 关闭 Follow

- [ ] 播放中打开 Follow 后，`C` 立刻绑定到 `<= R` 的最近过去逗号。
- [ ] 播放中关闭 Follow 后，`C` 保持当前值，不再继续追随 `R`。
- [ ] 播放期间 Timeline 始终聚焦 `R`，即使 `C` 正在被同步。

### 8.2 暂停态打开 / 关闭 Follow

- [ ] 暂停态切换 Follow 不会自动修改 `L / R / C`。
- [ ] 暂停态切换 Follow 不会改变当前聚焦对象。

### 8.3 播放中编辑文本

- [ ] 播放中且 Follow 关闭时，改文本后 `R` 继续推进，`C` 按当前文本光标重算。
- [ ] 播放中且 Follow 开启时，改文本后 `C` 会重新绑定到“当前文本中 `<= R` 的最近过去逗号”。
- [ ] 播放中点文本或用键盘移动光标，不会抢走 Timeline 对 `R` 的聚焦。

## 9. 播放快照与当前文本分离

- [ ] 开始播放后，记住当前画面/音效对应谱面内容。
- [ ] 播放过程中修改文本，Timeline 上的线和锚点会立刻变化。
- [ ] 但当前实际播放内容不会跟着文本编辑立刻改变。
- [ ] 只有停止并重新播放后，新的文本内容才会进入新的播放快照。
- [ ] 播放期间如果把谱面改短，`R` 仍可继续跑到旧快照的更晚时刻。
- [ ] 此时 Timeline 允许 `R` 落在当前文本末尾之后的空白区。

## 10. 无逗号 / 稀疏逗号边界

- [ ] 全文无逗号时：
  - [ ] `C` 始终为 `0`
  - [ ] 头部点击后 `R` 可移动，但 `C` 仍为 `0`
  - [ ] Follow 开启播放时，`C` 仍为 `0`
- [ ] 某一行无逗号但其起始时间早于点击秒数时，头部点击会回退到全局最近过去逗号。
- [ ] 若全局也无过去逗号，则头部点击后的 `C := 0`。

## 11. 视觉与聚焦回归

- [ ] 聚焦在 `C` 时，用户能明显看出当前中心是编辑器锚点而非播放头。
- [ ] 一旦执行纯 `R` 动作，聚焦会切回 `R`。
- [ ] 播放中永远以 `R` 为主聚焦对象。
- [ ] `L` 标记在缩放、拖动、seek、停止后都能保持正确位置。
- [ ] 缩放不会把 `C` 或 `R` 错误改值。

## 12. 回归风险点

- [ ] `Ctrl+点 Timeline` 不再跳最近 note。
- [ ] 普通点击 Timeline 头部仍能稳定驱动编辑器定位。
- [ ] `Ctrl+点文字` 仍能在播放中打断播放并执行 `C -> R`。
- [ ] Preview slider、Timeline 拖拽、头部点击三者都能正确暂停播放，但语义各自不同。
- [ ] 停止按钮语义是“回到 `L`”，不是“回到 0”。
- [ ] 自然结尾不再偷偷触发“停止”语义。

## 13. 建议记录模板

每条可记录：

- [ ] 是否通过
- [ ] 实际结果
- [ ] 预期结果
- [ ] 截图 / 录屏
- [ ] 是否可稳定复现
