# 导出视频当前模式详解与内存占用分析

## 目标

这份文档只解释当前 MiaCode 的导出模式。

重点回答两个问题：

1. 当前导出模式到底是怎么工作的
2. 为什么导出时会占用大量内存，以及为什么进度条越接近完成，内存通常越高

## 当前导出模式概览

当前导出模式可以概括为：

- 统一单一路径
- 固定使用当前默认导出算法
- 不再提供导出性能切换项
- 导出控制器内部直接按固定策略执行

当前相关入口主要在：

- `src/tools/video_export/VideoExportDialog.cpp`
- `src/tools/video_export/BatchVideoExportDialog.cpp`
- `src/app/main.cpp`
- `src/app/mainwindow/MainWindow.cpp`
- `src/tools/video_export/VideoExportController.cpp`
- `src/tools/video_export/VideoExportSnapshot.cpp`

## 当前导出模式的完整流程

## 1. 任务参数收集

导出一开始先收集任务参数，来源有三种：

- 单次导出对话框
- 批量导出对话框
- CLI 导出

无论入口来自哪里，最终都会归并到同一套 `VideoExportTask` 数据结构。

任务里包含的主要内容有：

- 输出路径
- 谱面路径
- 背景媒体路径
- 音频路径
- 皮肤目录
- note markers
- Muri 分析结果和渲染选项
- 音频设置
- 外侧和内侧亮度
- 判定线大小
- 背景缩放模式
- 流速
- 导出起点和导出时长
- 输出分辨率
- FPS
- 是否显示时间戳
- 是否显示物件统计

也就是说，当前导出模式不是“直接把预览窗口录下来”，而是：

- 先把当前导出所需状态整理成任务对象
- 再由导出控制器按照任务对象重新执行一套导出流程

## 2. 快照与任务重建

当前实现里，导出链路支持把任务内容序列化为导出快照，再在导出端重建任务。

快照主要负责保存：

- 谱面文本
- 难度
- 资源路径
- 音频设置
- 预览和渲染设置
- 导出时间范围
- 输出尺寸与 FPS

快照层的意义是：

- 把“导出需要的输入”明确固定下来
- 避免导出执行时继续依赖界面瞬时状态

这一层本身不是大内存来源，因为序列化出来的 JSON 相对很小。

真正的大头在后面的音频混音、渲染帧生成和编码阶段。

## 3. 导出控制器启动

当前主执行入口是：

- `VideoExportController::exportPreparedTask()`

这一步会做几类准备：

- 校验路径和输入合法性
- 选择编码器
- 计算编码线程和滤镜线程
- 建立导出画布
- 设置导出画布的渲染状态

当前固定策略下，控制器会：

- 使用当前默认线程规划
- 固定关闭 CPU 轨道区域缓存

对应设置是：

- `exportCanvas.setCpuTrackAreaCachingEnabled(false)`

这意味着当前导出模式不会再额外开启轨道区域图像缓存来换速度。

## 4. 导出画布状态构建

控制器会创建一个专门用于导出的 `PreviewCanvas`。

如果存在源预览画布，则会复制渲染状态；
如果不存在，则会自行加载皮肤和基础资源。

随后再把任务中的状态逐项灌进去，例如：

- 背景媒体可用性
- 背景亮度
- 判定线大小
- 平滑亮度
- 背景缩放模式
- 流速
- note markers
- Muri 选项
- Muri 分析结果
- 时间戳显示
- 物件统计显示

这一步之后，导出画布就进入“能按导出任务独立渲染每一帧”的状态。

## 5. 音频侧处理

当前导出模式的音频不是边渲染边临时拼一点点，而是会先准备音频资源。

音频部分通常包含：

- 主轨 `track.mp3`
- 导出区间对应的 SFX 混音

其中最重的一步是：

- `mixSfxTrackToWav()`

这一步会根据整段导出时长，先在内存中准备一整条 SFX 混音轨，再输出成 WAV 文件。

当前实现不是流式混音，而是整段预混，因此：

- 导出时长越长
- 音频缓冲越大

这是当前内存占用的核心来源之一。

## 6. 视频帧渲染

导出进入正式渲染时，会按目标 FPS 一帧一帧生成画面。

每一帧都不是简单的“对象列表”，而是完整像素图。

当前导出模式里，帧生成大致包含：

- 更新时间点
- 让导出画布渲染当前时刻
- 从离屏或导出画布拿到完整帧
- 生成或转换为 ffmpeg 需要的像素布局
- 写入编码进程

这里的关键点是：

- 每一帧都是整张图
- 分辨率越高，每张图越大
- 同一时刻可能会并存多份整帧缓冲

所以视频侧内存占用会随分辨率明显上升。

## 7. ffmpeg 编码

当前导出模式最终不是自己写 MP4，而是把视频帧和音频资源交给 ffmpeg 编码。

ffmpeg 侧会持有自己的内部状态，包括：

- 输入缓冲
- 滤镜缓冲
- 编码线程缓冲
- 码率控制和 lookahead
- 封装阶段的内部对象

这部分内存不是 MiaCode 显式 new 出来的大块数组，但依然是导出总内存的一部分。

## 8. 收尾与 remux

在进度接近结束时，导出不一定已经“只剩一点点”。

当前模式下，编码完成后通常还要做收尾动作，例如：

- 处理中间输出
- 进入 remux 阶段
- 生成最终输出文件
- 替换或清理临时文件

所以进度 90% 以后，系统里不只是“还剩最后几帧”，还可能同时存在：

- 编码阶段输出
- remux 阶段输出
- 系统文件缓存

这正是“越接近完成，内存反而越高”的重要原因之一。

## 当前模式下的主要内存来源

## 1. 整段 SFX 混音缓冲

当前最容易量化的大头就是音频预混。

相关函数：

- `mixSfxTrackToWav()`
- `writeWav16()`

当前流程是：

1. 先按总时长计算采样数
2. 分配一整块 `QVector<float>` 作为混音缓冲
3. 把所有导出区间内的 SFX 混进去
4. 再分配一整块 PCM 字节缓冲
5. 把 float 样本整体转成 16-bit PCM
6. 最后写 WAV

这意味着音频侧至少会同时存在两份大块数据：

- float 混音缓冲
- PCM 输出缓冲

如果按 48kHz、双声道、3 分钟估算：

- float 混音缓冲约 65.9 MiB
- PCM 缓冲约 33.0 MiB

光音频峰值就接近 100 MiB。

如果导出 5 分钟、8 分钟甚至更长，这部分会继续近似线性上涨。

## 2. 整帧 RGBA 图像缓冲

视频导出不是按对象流直接编码，而是先得到整帧像素图，再送给 ffmpeg。

因此 CPU 侧通常会存在：

- 当前渲染帧
- 转换后的 RGBA 帧
- 打包或临时写入帧

它们的大小直接受输出分辨率影响。

单帧 `RGBA` 体积大致为：

- `1024x1024`: 约 4.0 MiB
- `1920x1080`: 约 7.9 MiB
- `2560x1440`: 约 14.1 MiB
- `3840x2160`: 约 31.6 MiB

如果高分辨率下同时持有 2 到 3 份整帧缓冲，内存就会很快抬升。

## 3. 离屏 OpenGL 资源

当前导出模式使用离屏渲染路径，因此会持有一批 GPU 侧资源：

- FBO
- 渲染纹理
- 读回缓冲
- 可能的多重采样缓冲

这些资源虽然不一定全部反映到同一个“进程内存”数字上，但会体现在总资源占用里。

尤其在高分辨率时，离屏缓冲本身就不是小对象。

## 4. ffmpeg 内部缓存

ffmpeg 会根据编码器、滤镜、线程策略保留内部缓冲。

这部分往往表现为：

- 看起来 MiaCode 代码里没有那么多显式大数组
- 但任务管理器里的占用仍然很高

原因就在于编码和滤镜链本身也会消耗可观内存。

## 5. 导出期间逐步暖起来的渲染缓存

当前模式虽然已经关闭 CPU 轨道区域缓存，但仍然会使用一些正常的渲染缓存，例如：

- `overlayCache_`
- `animatedSpriteCache_`

这些缓存的特点是：

- 不是一开始全部分配
- 而是在遇到对应素材、组合或效果时逐步插入

因此导出后半段如果出现更多新的素材组合，缓存就会继续变大。

## 为什么进度越接近完成，内存越高

## 1. 后半段遇到更多未缓存内容

如果某些对象、效果、组合只在谱面后半段出现，那么对应缓存项也只会在后半段建立。

这会导致：

- 前半程内存较低
- 中后段逐步上升
- 临近结束时达到本次导出的最高工作集

这类上涨通常不是泄漏，而是缓存覆盖范围在扩大。

## 2. 缓存中途通常不会主动清理

当前一些缓存会在导出开始前或状态复制时清理，但不会在每一段导出中途频繁清掉。

这样做的原因是：

- 可以避免重复构建
- 维持导出过程稳定

代价就是：

- 本次导出见过的内容越多
- 进程内保留的缓存越多

## 3. 收尾阶段不只是“剩几帧”

很多时候进度条接近完成，实际上正在发生的是：

- 编码尾声
- 音视频封装
- remux
- 文件替换

这时磁盘写入量会继续增加，系统文件缓存也会相应增大。

如果观察的是任务管理器里的整体占用体感，往往会直接表现成：

- 最后 10% 看起来比前面更吃内存

## 4. 分配器和工作集回收不积极

导出过程中即使某些临时对象已经不用了，操作系统和分配器也不一定立即回收并把内存数字降下来。

因此会看到一种很常见的现象：

- 内存先涨上去
- 中途不怎么掉
- 导出完成后也未必立刻下降

这并不自动等于存在明显泄漏。

## 为什么当前更像“大缓冲叠加 + 缓存累积”

从当前代码路径看，更符合实际的描述是：

- 音频侧先整段分配大缓冲
- 视频侧每帧处理完整图像
- GPU 侧保持离屏资源
- ffmpeg 保留自己的编码与滤镜缓冲
- 渲染缓存按需增加

也就是说，当前问题更像：

- 大块内存本来就需要
- 再叠加随时间增温的缓存

而不是非常典型的：

- 每渲染一帧就永久泄漏一块不可回收对象

## 当前模式的实际优缺点

## 优点

- 导出路径统一，行为更稳定
- UI、CLI、批量导出共用同一套核心执行逻辑
- 导出路径不再分叉，维护面更小
- 关闭轨道区域 CPU 缓存后，长导出时的内存曲线会更稳一些

## 缺点

- 仍然保留整段 SFX 预混的大内存峰值
- 高分辨率导出时整帧缓冲压力仍然明显
- ffmpeg 和离屏渲染的资源占用仍然存在
- 导出后半程仍可能因为缓存暖起而上升

## 后续优化优先级

## 第一优先级：SFX 改为流式混音 / 流式写 WAV

这是最值得优先做的点。

如果把当前：

- 整段 `QVector<float>`
- 整段 `QByteArray`

改成：

- 分块混音
- 分块转 PCM
- 分块写出

那么音频侧峰值内存会明显下降，而且导出时长越长，收益越明显。

## 第二优先级：继续收紧编码和滤镜并发

如果仍需进一步压内存，可以继续评估：

- 降低滤镜线程
- 更严格控制软件编码线程
- 特定编码器下使用更保守策略

这通常会牺牲一部分导出速度。

## 第三优先级：高分辨率场景下进一步压缩离屏路径

例如：

- 更保守地使用离屏资源
- 减少同一时刻并存的中间图像副本
- 评估高分辨率下的读回策略

## 第四优先级：导出模式下的缓存上限或分段清理

如果后续确认某些缓存对长视频导出仍有明显累积影响，可以考虑：

- 给导出模式单独设置缓存上限
- 在特定区段主动清理部分缓存

但这类改动要非常谨慎，因为容易影响速度和渲染一致性。

## 最终结论

当前导出模式的本质是：

- 先把导出任务固定下来
- 构建独立导出画布和音频资源
- 逐帧渲染完整画面
- 交给 ffmpeg 编码
- 最后做收尾与 remux

当前内存占用高，主要来自：

- 整段 SFX 预混双缓冲
- 整帧图像缓冲
- 离屏 OpenGL 资源
- ffmpeg 内部缓存
- 导出过程中逐步增加的渲染缓存

而“越接近完成越高”的主要原因是：

- 后半段才出现的新缓存项
- 末尾 remux 和系统文件缓存
- 分配器与工作集不会积极回收

如果后续要真正显著降低导出峰值内存，最值得优先改造的仍然是：

- SFX 流式混音 / 流式写 WAV
